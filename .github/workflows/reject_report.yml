name: Reject Rate Daily Report

on:
  schedule:
    - cron: '20 7 * * *'
  workflow_dispatch:
    inputs:
      date:
        description: 'Date YYYYMMDD (leave empty for yesterday)'
        required: false

jobs:
  report:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - uses: actions/setup-python@v5
        with:
          python-version: '3.11'

      - run: pip install pymysql

      - run: python reject_daily_report.py ${{ inputs.date }}
        working-directory: Reject_daily
        env:
          DB_HOST: ${{ secrets.DB_HOST }}
          DB_PORT: ${{ secrets.DB_PORT }}
          DB_USER: ${{ secrets.DB_USER }}
          DB_PASSWORD: ${{ secrets.DB_PASSWORD }}
          DB_NAME: ${{ secrets.DB_NAME }}
          ADV_ID: '130010'
          ADV_NAME: 'Appnext-Click'
          REJECT_RATE_THRESHOLD: '0.05'
          PUB_MAPPING: ${{ secrets.PUB_MAPPING }}
          FEISHU_WEBHOOK: "https://open.feishu.cn/open-apis/bot/v2/hook/3e825272-d3a9-4f3b-b16f-fff6e8bd4f81"
